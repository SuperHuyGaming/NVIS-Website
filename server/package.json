{
	"scripts": {
		"server": "nodemon server.js",
		"build": "npm install --prefix ./client && npm run build --prefix ./client && npm install"
	},
	"type": "module",
	"dependencies": {
		"axios": "^1.10.0",
		"cors": "^2.8.5",
		"express": "^5.1.0"
	}
}
